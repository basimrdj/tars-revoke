# Billing observability

The billing service emits request counts, contract-validation failures, and
migration duration. Alerts are intentionally owned by a documentation-only task
in the TARS REVOKE demo so its Git lineage has no dependency on customer ID
serialization.
