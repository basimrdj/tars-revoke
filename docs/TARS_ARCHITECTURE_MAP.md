# TARS Architecture Map

This legacy filename is kept as a pointer for older references. The current
public architecture document is:

- `docs/architecture.md`

Use measurable terms when describing the system: event bus, appraisal, global
workspace, typed memory, sleep/consolidation, world-model prediction,
self-model metrics, and evaluation.
