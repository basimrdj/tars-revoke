# VibeVoice Voices

Downloaded `.pt` voice assets are intentionally not committed. Place local voice
files in this directory if you use the VibeVoice path, or set
`VIBEVOICE_MODEL_PATH` / `VIBEVOICE_VOICE` in your local `.env`.
